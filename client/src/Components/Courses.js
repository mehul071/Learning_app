import React from "react";
import Table from "react-bootstrap/Table";

function Courses({ course }) {
  //   const { course_name, description, madeby } = course;
  console.log(course.course_name);
  return <div></div>;
}

export default Courses;
